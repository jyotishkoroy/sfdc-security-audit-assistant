# Local dev notes

- LWC unit tests: `npm test`
- Lint: `npm run lint`

If you later deploy to an org, add the component to an App Page:
- Setup → Lightning App Builder → App Page → "Security Audit Assistant"
